[project]
name = "mcp-ai-dashboard-backend"
version = "0.1.0"
description = "FastAPI backend for MCP AI Dashboard"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
]
